import {  StyleSheet } from 'react-native'

export const Homestyles = StyleSheet.create({
    container: {
      
    },

    wrapper: {
        paddingTop: 80,
        paddingHorizontal: 40,
    },

    titleText: {
        fontSize: 18,
        fontWeight: 'bold',
    },

    NameText: {
        fontSize: 25,
    },

    image: {
        flex: 1,
        resizeMode: 'cover',
    }
});